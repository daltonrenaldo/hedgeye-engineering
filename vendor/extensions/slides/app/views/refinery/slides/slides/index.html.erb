<% content_for :body do %>
  <ul id="slides">
    <% @slides.each do |slide| %>
      <li>
        <%= link_to slide.title, refinery.slides_slide_path(slide) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
